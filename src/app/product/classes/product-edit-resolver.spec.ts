import { ProductEditResolver } from './product-edit-resolver';

describe('ProductEditResolver', () => {
  it('should create an instance', () => {
    expect(new ProductEditResolver()).toBeTruthy();
  });
});
