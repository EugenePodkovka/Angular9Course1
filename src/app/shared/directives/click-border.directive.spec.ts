import { ClickBorderDirective } from './click-border.directive';

describe('ClickBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickBorderDirective();
    expect(directive).toBeTruthy();
  });
});
